# 网络命令生成工具 — 仿制规划文档

> 参考站点：https://qsxwl.com（轻松学网络）
> 创建日期：2026-05-26
> 最后更新：2026-05-30
> 项目代号：**NetCmdGen**
> 许可协议：MIT（完全开源）

---

## 一、项目定位

### 1.1 目标用户
- 网络运维工程师（中小企业 IT、集成商现场工程师）
- 网络初学者、考证学员（HCIA/HCIP/CCNA/CCNP）
- 网络专业学生、培训机构

### 1.2 核心价值
**"画图勾选 → 一键生成多厂商命令"** —— 把繁琐的命令行工作图形化，降低多厂商配置门槛。

### 1.3 与原站差异化思路（避免直接侵权 + 形成竞争力）
| 维度 | 原站 qsxwl | 本项目（建议） |
|---|---|---|
| 形态 | Web SaaS | Web SaaS + 离线桌面版 |
| 拓扑画图 | 较基础 | 引入 drawio 级体验 + 自动布局 |
| AI 能力 | 无 | LLM 辅助：自然语言→命令、命令解析、配置审计 |
| 命令解析 | 中文注释 | 中文注释 + 命令对比 + 风险提示 |
| 开放度 | 闭源付费 | 完全开源（MIT），社区共建命令模板 |
| 协作 | 单人 | 多人协作画图、版本管理 |

---

## 二、原站功能拆解

### 2.1 站点结构
```
首页（newindex）
├── 顶部 Hero：标语 + CTA（自助开通 / 已开通登录）
├── 视频演示区
├── 系统功能（9 张卡片：交换机/路由器/防火墙/运维/画图/学习/规划中…）
├── 技术优势 + 客服二维码
└── 网络工具（IP 转换、子网计算、视频教程）
```

### 2.2 核心功能模块
1. **拓扑画图器**：拖拽设备（路由器/交换机/防火墙/PC/监控）+ 连线
2. **参数勾选面板**：基于端口/IP/MAC 划 VLAN、聚合、DHCP、生成树、VRRP+MSTP、OSPF…
3. **多厂商命令引擎**：13 厂商（华为/H3C/思科/锐捷/迈普/海康/TP-LINK/烽火/迪普/浪潮/博达/瑞斯康达/神州数码）
4. **中文解析**：每条命令逐行注释
5. **附赠资源**：ENSP 实验包、CRT 工具、视频课程

### 2.3 命令覆盖矩阵（待支持功能）
- **交换机**：VLAN（端口/IP/MAC）、IP 路由、端口聚合、DHCP、防私接小路由、远程管理、生成树、IP+端口绑定、静态路由、端口镜像、网段隔离、VRRP+MSTP、OSPF、策略路由
- **路由器**：拨号/固定 IP 上网、端口映射、静态路由、OSPF、IP 黑名单、多运营商分流、远程管理
- **防火墙**：接口区域、域间策略、拨号/固定 IP、端口映射、静态路由、OSPF、远程管理

---

## 三、技术架构

### 3.1 技术选型
| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | **Vue 3 + Vite + Element Plus** | 国内生态成熟，与原站风格契合 |
| 画图引擎 | **AntV X6** 或 **drawio-mxgraph** | X6 更轻量、可定制；drawio 体验最佳 |
| 状态 | Pinia | Vue 官方推荐 |
| 后端 | **Python FastAPI** | 生态丰富、AI 集成方便、开发效率高 |
| 命令引擎 | **Adapter 模式 + Python 函数式**（详见§五） | 多厂商可扩展，比模板更灵活 |
| DB | 无需数据库（文件存储） | 拓扑图、配置文件本地 JSON 存储，降低运维成本 |
| 文件存储 | 本地 JSON 文件 | 用户拓扑图、生成的配置文件 |
| 部署 | Docker + Nginx + 阿里云轻量 | 成本可控 |

### 3.2 系统架构图
```
[浏览器]
   │  HTTPS
   ▼
[Nginx]──静态资源
   │
   ▼
[API 网关 / Gin]──┬──[拓扑服务] ── 本地 JSON 存储
                  ├──[命令引擎] ── 模板库(YAML/JSON)
                  └──[资源服务] ── OSS(视频/工具包)
```

---

## 四、数据库设计（核心表）

> 注：当前阶段不需要数据库，拓扑图和命令模板均用 JSON/YAML 文件存储。
> 以下为未来可选扩展时的表结构设计。

```sql
-- 拓扑图（JSON 存图形 + 配置，本地文件存储）
topologies(id, name, graph_json, params_json, updated_at)

-- 厂商
vendors(id, code, name, device_types) -- huawei/h3c/cisco…

-- 命令模板（核心）
cmd_templates(
  id, vendor_id, device_type, feature_code,
  params_schema_json,   -- 参数定义
  template,             -- Jinja/Go-Template 文本
  cn_annotation,        -- 中文注释模板
  version, updated_at
)

-- 生成记录（本地缓存）
gen_history(id, topology_id, vendor, output, created_at)
```

---

## 五、命令生成引擎（最核心模块）

### 5.1 设计思路
**特性 (feature) × 厂商 (vendor) → 模板 (template)** 的二维矩阵，模板用占位符渲染。

### 5.2 模板示例（YAML，仅作格式示意）
```yaml
feature: vlan_by_port
vendors:
  huawei:
    template: |
      vlan {{ vlan_id }}
       description {{ desc }}
      interface {{ port }}
       port link-type access
       port default vlan {{ vlan_id }}
    annotations:
      - "vlan {{ vlan_id }}: 创建 VLAN"
      - "port link-type access: 设置端口为接入模式"
  h3c:
    template: |
      vlan {{ vlan_id }}
       description {{ desc }}
      interface {{ port }}
       port link-type access
       port access vlan {{ vlan_id }}
  cisco:
    template: |
      vlan {{ vlan_id }}
       name {{ desc }}
      interface {{ port }}
       switchport mode access
       switchport access vlan {{ vlan_id }}
```

### 5.3 引擎流程
```
拓扑图 JSON  ─┐
参数勾选数据 ─┼─► 参数归一化 ─► 选择厂商模板 ─► 渲染 ─► 拼接注释 ─► 输出
设备型号    ─┘
```

### 5.4 复杂特性策略
- VRRP+MSTP、OSPF 等多设备协同特性 → 在拓扑层面识别"逻辑组"，遍历每台设备调用对应模板
- 提供 **DSL 校验**：参数缺失/冲突预警（例如 VLAN ID 范围、端口名格式）

---

## 六、拓扑编辑器详细设计（核心模块）

> 参考效果图：类似 draw.io / ProcessOn 的专业拓扑画图体验。

### 6.1 布局结构
```
┌─────────────┬──────────────────────────────┬─────────────────────┐
│  左侧        │         中部画布              │   右侧               │
│  设备库      │                              │   参数面板            │
│  (Stencil)   │   ┌──────────────────┐       │                     │
│              │   │  点网格背景       │       │   设备属性            │
│  ▼ 网络设备  │   │                  │       │   - 主机名           │
│    ▧ 防火墙  │   │  [防火墙] [交换机]│       │   - IP 地址          │
│    ▨ 路由器  │   │     │VLAN10  │    │       │   - 接口列表          │
│    ▩ 交换机  │   │  [接入层]       │       │                     │
│    ▭ 无线AP │   │                  │       │   配置模板            │
│              │   └──────────────────┘       │   - VLAN             │
│  ▼ 终端设备  │                              │   - OSPF             │
│    ■ 服务器  │   ┌────────────────┐         │   - 端口聚合          │
│    □ PC      │   │  缩略图导航     │         │                     │
│              │   └────────────────┘         │  命令预览            │
│  ▼ 云/网络   │                              │   ┌──────────────┐   │
│    ▲ 互联网  │                              │   │ sysname xxx   │   │
│    ● 云服务  │                              │   │ vlan 10       │   │
└─────────────┴──────────────────────────────┴─────────────────────┘
│                                                                    │
│                    底部面板：多厂商命令输出                           │
│  ┌─ 华为 ─┬─ H3C ──┬─ 思科 ──┬─ 锐捷 ──┐                         │
│  │ vlan 10│ vlan 10│ vlan 10 │ vlan 10 │                         │
│  │ ...    │ ...    │ ...     │ ...     │                         │
│  └────────┴────────┴─────────┴─────────┘                         │
└────────────────────────────────────────────────────────────────────┘
```

### 6.2 设备图标库（共 20+ 种）

#### 网络设备
| 图标 | 设备类型 | 描述 | 状态 |
|---|---|---|---|
| ▧ | 防火墙 | 安全网关（图片中橙色砖墙样式） | ⚠️ 待扩充 |
| ▨ | 核心交换机 | 三层核心交换机 | ⚠️ 待扩充 |
| ▨ | 汇聚交换机 | 二层汇聚交换机 | ✅ 已有 |
| ▨ | 接入交换机 | 二层接入交换机 | ✅ 已有 |
| ▩ | 路由器 | 企业路由器/软路由 | ✅ 已有 |
| ▭ | 无线AP | 胖/瘦 AP | ❌ 待新增 |
| ▭ | AC 控制器 | 无线控制器 | ❌ 待新增 |
| ▭ | 天线 | 无线天线 | ❌ 待新增 |

#### 终端设备
| 图标 | 设备类型 | 描述 | 状态 |
|---|---|---|---|
| ■ | 服务器 | 通用服务器 | ✅ 已有 |
| □ | PC | 个人电脑 | ✅ 已有 |
| □ | 笔记本 | 笔记本电脑 | ❌ 待新增 |
| □ | 打印机 | 网络打印机 | ❌ 待新增 |
| ■ | 网络存储 | NAS / SAN | ❌ 待新增 |

#### 云端/运营商
| 图标 | 设备类型 | 描述 | 状态 |
|---|---|---|---|
| ▲ | 互联网 | 公网/Internet | ❌ 待新增 |
| ● | 云服务 | 公有云 | ❌ 待新增 |
| ▲ | 运营商专线 | MPLS / IPLC 等 | ❌ 待新增 |
| ● | 光端机 | 光纤收发器 | ❌ 待新增 |

#### 安全类
| 图标 | 设备类型 | 描述 | 状态 |
|---|---|---|---|
| ▧ | 行为管理 | 上网行为管理 | ❌ 待新增 |
| ▧ | 网闸 | 网络安全隔离设备 | ❌ 待新增 |
| ▧ | IDS/IPS | 入侵检测/防御 | ❌ 待新增 |

### 6.3 画布功能清单

| 功能 | 描述 | 优先级 | 预估工时 |
|---|---|---|---|
| **网格背景** | 点状/线状网格（X6 原生支持） | P0 | 0.5h |
| **设备拖入** | 从左侧 Stencil 拖拽到画布 | P0 | 1d（已有基础） |
| **设备连线** | 拖拽端口产生连接线 | P0 | 1d（已有基础） |
| **连线标签** | 线旁标注 VLAN 号、IP 网段、链路类型 | P1 | 1d |
| **分组容器** | 虚线框包裹多设备（如"总部网络"） | P1 | 1.5d |
| **缩略图导航** | 右下角 MiniMap 小地图 | P1 | 0.5d |
| **属性面板** | 点击设备展示 IP、主机名等可编辑属性 | P1 | 1d |
| **自动布局** | 核心-汇聚-接入三层拓扑自动排列 | P2 | 2d |
| **撤销/重做** | Ctrl+Z / Ctrl+Y | P2 | 1d |
| **导入/导出** | JSON 文件导入导出拓扑 | P2 | 1d |
| **导出 PNG** | 将拓扑图导出为图片 | P3 | 0.5d |

### 6.4 拓扑与命令引擎的集成方案

```
用户操作流程：
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 拖入设备  │ → │ 配置属性  │ → │ 选择厂商  │ → │ 生成命令  │
│ 连线拓扑  │    │ 勾选特性  │    │ 切换对比  │    │ 复制导出  │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
      ↓              ↓              ↓              ↓
 拓扑 JSON    参数 JSON      vendor 参数     命令文本
```

**关键集成点：**
1. 拓扑画布保存时输出标准 JSON（设备类型、连线关系、IP 分配）
2. 右侧参数面板根据设备类型动态展示可配置的特性（交换机↦VLAN/STP，路由器↦OSPF/BGP）
3. 点击"生成命令"时，遍历拓扑中的每个设备，根据其类型和角色调用对应 Adapter
4. 底部输出面板按厂商 Tab 分页显示，支持一键复制、对比查看

### 6.5 图标资源方案
- **首选**：从 draw.io 开源图标集提取（Apache 2.0 协议，约 500+ 网络图标）
- **备选**：使用 iconfont 图标库（阿里矢量图标库，有网络设备分类）
- **格式**：统一用 SVG，尺寸 48×48 或 64×64
- **路径**：`web/src/assets/icons/devices/`

---

## 七、UI 设计要点（参考原站，但需原创素材）

### 7.1 配色与风格
- 主色：科技蓝 `#1e88e5` / 强调橙 `#ff7043`
- 风格：扁平 + 卡片化 + 大量原创插画/截图
- 字体：思源黑体 / PingFang SC

### 7.2 关键页面
1. **首页 (Landing)**
   - Hero：原创标语 + 主图 + CTA 按钮（开始使用）
   - 功能卡片网格（9 宫格）
   - 客服二维码 + 底部导航
2. **画图工作台**（左侧设备库 / 中部画布 / 右侧参数面板 / 底部命令输出）
3. **网络工具页**（IP 转换、子网计算 — 引流入口）
4. **视频教程页**（分类播放）

### 7.3 移动端
- 仅支持"查看 + 复制命令"，画图功能保留 PC 端

---

---

## 八、项目现状诊断（2026-05-30）

> 代码探查结论：已有良好的基础框架，但存在"Adapter 层断连"的核心问题。

### 8.1 已完成功能

| 模块 | 完成度 | 说明 |
|---|---|---|
| 命令引擎 - 华为 | **40%** | Basic + VLAN 可用，Routing/Security/Interface/QoS 底层代码已写但 Adapter 未接入 |
| 命令引擎 - H3C | **100%** | 全部 6 大特性可用 |
| 命令引擎 - 锐捷 | **30%** | Basic + VLAN 可用，底层有 29 个方法但 Adapter 未接入 |
| 命令引擎 - 迈普 | **30%** | 同锐捷 |
| 网络工具集 | **100%** | 9 大工具全部可用（子网/Ping/Trace/端口扫描/DNS/Whois/MAC/IP/编码） |
| 命令速查库 | **100%** | 4 厂商 2500+ 条命令数据完备 |
| 拓扑编辑器 | **50%** | 基础拖拽+连线可用，缺少图标库/连线标签/分组/缩略图等 |
| 前端参数面板 | **30%** | 仅 5 个固定字段，缺少按设备类型动态表单 |

### 8.2 核心问题：Adapter 断连

**现象**：Adapter 层大量 `generate_*` 方法返回占位符字符串，但对应 `vendors/` 目录下的底层生成器代码已经完整实现。

```
Adapter 层（适配器接口）       Vendors 层（底层生成器）
─────────────────────────     ────────────────────────
HuaweiAdapter._gen_routing()   vendors/huawei/routing.py  ← 8KB 代码已就绪
  → 返回 "# routing 模板待接入"    HuaweiRoutingGenerator.gen_ospf()
                                HuaweiRoutingGenerator.gen_static_route()
                                
HuaweiAdapter._gen_security()  vendors/huawei/security.py  ← 23KB 代码已就绪
  → 返回 "# security 模板待接入"   HuaweiSecurityGenerator.gen_acl()
                                HuaweiSecurityGenerator.gen_nat()
                                
RuijieAdapter._gen_routing()   vendors/ruijie.py  ← 29 个完整方法
  → 返回 "! routing 模板待接入"     generate_ospf(), generate_bgp(), generate_stp()...
```

**影响**：约 60KB 的生成器代码处于闲置状态，只需完成 Adapter 层的"接线"即可激活。

**优先级**：⭐ 最高 — 不是写新代码，是桥接已有代码。预计 16 个方法，1 周可完成。

### 8.3 其他已知问题

| 问题 | 严重度 | 说明 |
|---|---|---|
| 前端参数与后端不匹配 | 🟡 中 | `Generator.vue` 发扁平参数，H3C 期望结构化参数 |
| 园区网模板割裂 | 🟡 中 | Campus 模板绕过 Adapter，只支持 huawei/h3c |
| 安全风险 | 🔴 高 | 端口扫描器无白名单/限频，可能被滥用 |
| Python 依赖不锁版本 | 🟢 低 | `requirements.txt` 仅 4 个依赖，未锁定上限 |

---

## 九、分阶段开发计划

> 总工期估算：单人约 **10-12 周** / 2-3 人团队约 **5-6 周**

### 第一阶段：Adapter 桥接 + 拓扑编辑器基础（第 1-2 周）

**目标**：把现有 70% 的代码变成真正可用，拓扑编辑器达到可用水平。

| 周次 | 任务 | 详细说明 | 交付物 |
|---|---|---|---|
| **W1** | Adapter 桥接（华为） | 接入 Routing/Security/Interface/QoS 四个模块，映射参数格式 | 华为 6/6 特性可用 |
| **W1** | Adapter 桥接（锐捷、迈普） | 接入各自 29 个底层方法中的 routing/security/interface/service | 锐捷、迈普各 6/6 特性可用 |
| **W1** | 前端参数面板改造 | 改为按 device_type 动态渲染表单（交换机↦VLAN/STP，路由器↦OSPF/BGP） | 动态参数面板 |
| **W2** | 图标库扩充 | 从 draw.io 提取 20+ 网络设备 SVG 图标 | `assets/icons/devices/` |
| **W2** | 左侧 Stencil 面板 | 实现分层可拖拽设备库（网络设备/终端/云/安全） | 图形库面板 |
| **W2** | 连线标签 | 连线支持标注 VLAN 号、IP 网段、链路类型 | 标签连线 |

### 第二阶段：拓扑编辑器进阶（第 3-4 周）

**目标**：达到参考图片中 draw.io 级别的画图体验。

| 周次 | 任务 | 详细说明 | 交付物 |
|---|---|---|---|
| **W3** | 分组容器 | 虚线框包裹多设备，支持命名（如"总部网络"） | Parent-Child 分组 |
| **W3** | 缩略图导航 | 右下角 MiniMap 小地图 | X6 MiniMap 集成 |
| **W3** | 属性编辑面板 | 点击设备弹出/侧边栏显示属性（主机名/IP/接口列表），可编辑 | 属性面板 |
| **W4** | 自动布局 | 接入 dagre 算法，核心-汇聚-接入三层自动排列 | 一键布局 |
| **W4** | 撤销/重做 | X6 History 插件，支持 Ctrl+Z/Y | 操作历史 |
| **W4** | 导入/导出 | 拓扑 JSON 文件导入导出 | 文件操作 |

### 第三阶段：拓扑 ↔ 命令引擎集成（第 5-6 周）

**目标**：打通"画拓扑 → 选厂商 → 生成命令"的完整流程。

| 周次 | 任务 | 详细说明 | 交付物 |
|---|---|---|---|
| **W5** | 拓扑设备识别 | 根据拓扑 JSON 识别每台设备的类型和角色（核心/汇聚/接入） | 设备分析器 |
| **W5** | 特性自动推荐 | 交换机自动推荐 VLAN/STP/聚合，路由器推荐 OSPF/BGP | 智能推荐 |
| **W5** | 底部命令面板 | 多厂商 Tab 分页，显示生成命令，支持一键复制 | 命令输出面板 |
| **W6** | 多设备协同配置 | VRRP 主备配对、MSTP 域配置 等跨设备逻辑 | 协同生成 |
| **W6** | 命令对比视图 | 同一功能多厂商命令并排对比 | 对比面板 |
| **W6** | 参数校验增强 | VLAN ID 范围校验、IP 子网冲突检测、端口名格式校验 | DSL 校验 |

### 第四阶段：页面 + 工具完善（第 7-8 周）

**目标**：补齐页面和用户体验。

| 周次 | 任务 | 详细说明 | 交付物 |
|---|---|---|---|
| **W7** | 首页 Landing | Hero 标语、功能卡片、技术优势、底部导航 | 首页 |
| **W7** | 网络工具页 | 整合已有 9 个工具到美观页面 | 工具页 |
| **W7** | 命令速查页 | 整合已有 4 厂商 2500+ 命令数据到搜索页面 | 命令速查页 |
| **W8** | 安全加固 | 端口扫描器加白名单/限频；Ping/Trace 加超时控制 | 安全加固 |
| **W8** | 移动端适配 | "查看+复制命令"的响应式布局 | 移动端 |

### 第五阶段：测试、打磨、上线（第 9-10 周）

| 周次 | 任务 | 详细说明 | 交付物 |
|---|---|---|---|
| **W9** | 命令准确性验证 | 至少对华为/H3C 核心命令做 ENSP 模拟器验证 | 测试报告 |
| **W9** | 兼容性测试 | Chrome/Edge/Firefox；Windows/macOS | 兼容性清单 |
| **W10** | Docker 部署 | Dockerfile + docker-compose + Nginx 反向代理 | 部署方案 |
| **W10** | 备案上线 | ICP 备案、SEO 优化、README 文档 | 正式上线 |

### 🔮 未来规划（v2.0+）

| 功能 | 说明 |
|---|---|
| AI 自然语言生成命令 | 基于已有 2500+ 命令数据做 RAG，LLM 辅助生成 |
| 更多厂商 | 思科/Juniper/TP-LINK/烽火/海康等续补到 13 厂商 |
| 多人协作 | WebSocket 实时同步拓扑编辑 |
| 拓扑版本管理 | Git-like 的拓扑历史与回滚 |
| 离线桌面版 | Electron 打包，支持完全离线使用 |

---

## 十、竞品调研与借鉴（2026-05-31）

> 联网调研了多个开源网络运维工具，提取值得借鉴的功能点。

### 10.1 调研工具清单

| 工具 | 形态 | 核心亮点 |
|---|---|---|
| **NetOps Toolkit v4.0** | Python + PyQt5 桌面应用 | 深色渐变主题、Tab 分区、1000+ 命令速查、35+ 配置案例、厂商间命令转换 |
| **OpsMate 网络运维工具箱** | 桌面应用 | 内置 SSH/Telnet 终端、批量命令执行、IP/端口扫描、子网划分 |
| **qsxwl.com（轻松学网络）** | Web SaaS | 拓扑画图 → 勾选参数 → 多厂商命令生成，全程图形化 |
| **Net Tools (mefcl)** | Windows 桌面 | Speed Test 测速、远程命令记录 Markdown 存储、路由追踪可视化 |
| **52pojie 交换机批量配置生成器** | 桌面脚本 | Excel CSV 变量批量替换、100 台设备秒级生成配置 |
| **net-genconfig (PyPI)** | Python 库 + Web | Jinja2 模板 + inventory 数据 → 配置渲染，支持 SDN 环境 |

### 10.2 最值得借鉴的核心功能

#### 🔥 P0（直接增强核心能力）

| 功能 | 来源 | 当前缺失 | 实现方案 |
|---|---|---|---|
| **配置案例模板库** | NetOps Toolkit（35+案例）| ❌ 无 | 预设场景 JSON 文件，一键加载预填参数。如"园区网核心交换机开局模板"包含 VLAN/STP/OSPF 典型值 |
| **批量设备生成配置** | 52pojie / OpsMate | ❌ 无 | 上传 Excel/CSV，每行一台设备（主机名/IP/VLAN等），一次生成所有配置 |
| **配置对比/厂商翻译** | NetOps Toolkit | ⚠️ 有对比Tab | 同一功能华为↔思科命令并排，高亮差异行。输入华为命令→输出H3C等价格式 |
| **导出多种格式** | NetOps Toolkit | ⚠️ 仅 JSON/复制 | 导出为 .cfg / .txt / Excel / Markdown，带设备名文件名 |

#### 🟡 P1（扩展核心能力边界）

| 功能 | 来源 | 说明 | 实现方案 |
|---|---|---|---|
| **内置 SSH/Telnet 终端** | OpsMate | 不离开工具即可连接设备 | Web 端用 xterm.js + WebSocket 后端代理，直接输入命令 |
| **配置下发** | 52pojie v2.0 | 生成命令后直接推送到设备 | 后端 paramiko SSH 连接设备，逐条执行配置命令 |
| **配置巡检/差异检测** | 52pojie / NetOps Toolkit | 对比当前配置 vs 标准模板 | 定义"基线配置模板"，巡检时拉取设备 running-config 做 diff |
| **配置历史回滚** | Net Tools (Markdown) | 每次生成自动记录 | 本地 localStorage 存储生成历史，可按时间/设备名搜索 |
| **实时设备监控** | OpsMate / 商业网管 | Ping 不通告警、端口状态检查 | 后端定时轮询设备状态，前端仪表盘展示 |

#### 🔵 P2（差异化竞争点）

| 功能 | 来源 | 说明 |
|---|---|---|
| **拓扑自动发现** | NetOps + LLDP | 连接核心交换机，解析 LLDP 邻居自动画拓扑 |
| **AI 审计** | 我们的优势 | LLM 分析生成命令，标注风险（如 `no spanning-tree` 会导致环路） |
| **多人协作拓扑** | 独有 | WebSocket 实时同步，多人同时编辑同一拓扑 |

### 10.3 推荐下一阶段开发路线

```
现有 ──► 案例模板库 ──► 批量设备生成 ──► 配置导出 ──► 命令对比
  │           1周            1周           0.5周       0.5周
  │
  └──► SSH终端嵌入 ──► 配置下发 ──► 配置巡检 ──► AI审计
          2周           1.5周          2周          2周
```

### 10.4 差异化策略

| 我们独有的 | 竞品没有 | 价值 |
|---|---|---|
| Web 浏览器即用 | 多数需安装桌面软件 | 零门槛，分享链接即可 |
| 拓扑画图 → 命令引擎联动 | 多数仅表单填写 | 视觉化，减少配置遗漏 |
| 连线精细控制（端口/MTU/OSPF区域） | 无此粒度 | 专业场景覆盖 |
| 多厂商命令实时对比 | 少数有但不够直观 | 降低多厂商学习成本 |

---

## 十一、风险与合规

### 11.1 法律风险（重要！）
- **不得直接复制**原站文案、图片、视频、CSS 样式表。所有 Hero 标语、卡片配图必须重新设计/原创撰写
- ENSP 模拟器、CRT 等**不能直接打包分发**，应改为"提供官方下载指引"
- 软著最好登记，避免反被诉抄袭
- 备案：ICP 备案 + 公安备案；涉及视频教学需考虑《网络视听许可证》（个人可先用第三方平台嵌入）

### 11.2 命令准确性风险
- 多厂商命令差异极大，**必须有真机/模拟器验证流程**
- 引入"命令模板版本"概念，错命令可快速回滚
- 提供"用户报错反馈"入口，社区共建

### 11.3 项目策略
- 纯模仿难有竞争力，**务必走差异化**（AI / 协作 / 开源生态）
- 完全开源免费，靠社区贡献驱动命令模板增长

---

## 十二、当前 Sprint 行动清单

> 最后更新：2026-06-02  当天 Sprint 7+8+9 完成

### ✅ Sprint 1-6：参见上方开发日志（已完成）

### ✅ Sprint 7：P0 增强（已完成）
- [x] 撤销/重做（Ctrl+Z/Y）— X6 History 插件 + 快捷键
- [x] 导出 PNG 图片 — X6 exportPNG()
- [x] 导出 Excel — xlsx 库多厂商对比表
- [x] 模板扩充 — 从 15 套扩到 30 套（华为/华三/锐捷/迈普全覆盖）

### ✅ Sprint 8：RouterOS (MikroTik) 全线接入（已完成）
- [x] 后端：RouterOS 命令生成引擎（basic/vlan/routing/security/interface/qos）V6/V7 双版本
- [x] 后端：RouterOS 适配器 + 工厂注册（factory.py, generate.py）
- [x] 后端：RouterOS 命令速查数据（86 条，含 V6/V7 版本标注）
- [x] 前端：厂商列表 + V6/V7 版本选择器 + RouterOS SVG 图标
- [x] 前端：RosBasicForm / RosInterfaceForm / RosRoutingForm / RosFirewallForm 专属表单
- [x] PCC 多线负载均衡完整实现（对标 WinBox）：Mangle 标记 + 连接分类器 + Bucket 分配 + V7 routing table
- [x] 模板：RouterOS V6 基础 + V7 OSPF 两套模板

### ✅ Sprint 9：版本命令完整性 + 路由器/交换机分离（已完成）
- [x] 命令速查：华为/H3C/锐捷/迈普/RouterOS 全部支持版本过滤
- [x] 华为 ACL 安全模块版本区分（V5 `acl number` → V8/V300 `acl`）
- [x] 华三 Comware V5/V7 版本区分（ACL/SSH 语法差异）
- [x] 华为路由器 WAN 生成器（PPPoE/静态IP/DHCP/NAT/DNAT/DHCP-Server）
- [x] 华三路由器 WAN 生成器（dialer-group + nat outbound + DNAT）
- [x] 命令准确性验证：对照华为 AR 路由器官方文档修正 ACL/PPPoE/DNAT 语法
- [x] 路由器 ↔ 交换机 Tab 分离：路由场景显示 WAN/ACL/DHCP，交换机场景显示 VLAN/QoS
- [x] 四厂商专属路由表单：华为(OSPF/BGP/RIP/VRRP)、华三(OSPF/BGP/VRRP)、锐捷/迈普(OSPF/BGP)
- [x] 全小白友好改造：逐步向导卡片 + 每字段 ? 提示 + 实时命令预览 + Web 配置指引

---

## 十三、2026-06-01 开发日志（完整记录）

> 本日累计修改/新增文件：**约 25 个文件**

### 一、后端 Adapter 桥接

| 文件 | 操作 | 说明 |
|---|---|---|
| `api/app/engine/adapters/huawei.py` | 修改 | 接入 Routing/Security/Interface/QoS 四大模块 |
| `api/app/engine/adapters/ruijie.py` | 修改 | 接入 routing/security/interface/service（29 个方法） |
| `api/app/engine/adapters/maipu.py` | 修改 | 同上，结构与锐捷一致 |
| `api/app/engine/vendors/huawei/version_config.py` | **新增** | VRP 版本命令映射（V5/V8/V300） |
| `api/app/engine/vendors/huawei/basic.py` | 修改 | 支持版本参数（SSH/NTP/用户/密码按版本区分） |
| `api/app/engine/vendors/huawei/security.py` | 修改 | 修复 ACL rule ID 为 None 的 bug |
| `api/app/engine/vendors/huawei/interface.py` | 修改 | 修复 LLDP `{interval}` 未插值 |
| `api/app/api/generate.py` | 修改 | API 新增 `vrp_version` 字段 |

### 二、前端图标库（14 个新 SVG）

| 分类 | 图标 |
|---|---|
| 无线 | `antenna.svg` `wireless-ap.svg` `wireless-ac.svg` |
| 终端 | `laptop.svg` `printer.svg` `nas.svg` `ip-phone.svg` `camera-monitor.svg` `storage.svg` |
| 云/运营商 | `internet.svg` `optical-transceiver.svg` |
| 安全 | `behavior-management.svg` `data-diode.svg` `ids-ips.svg` |

### 三、前端新增/重写组件

| 文件 | 操作 | 说明 |
|---|---|---|
| `components/generator/BasicForm.vue` ~ `ServiceForm.vue`（7 个） | **新增** | 按设备类型动态渲染的配置表单 |
| `components/generator/ProjectView.vue` | **新增** | 拓扑项目管理（localStorage + MD 导出） |
| `components/generator/deviceIcons.ts` | 重写 | 22 种设备 + 4 大分组 + DeviceGroup 接口 |
| `components/generator/DevicePalette.vue` | 重写 | 折叠面板分组 + 搜索过滤 |
| `data/templates/*.json`（4 个） | **新增** | 园区核心/接入/路由器/防火墙预设模板 |
| `data/templates/index.ts` | **新增** | 模板加载查询模块 |

### 四、前端核心页面修改

| 文件 | 改动 |
|---|---|
| `views/Generator.vue` | **完全重写 2 次** — Tab 切换 + 批量/对比/拓扑导入/版本选择/模板加载/导出 .cfg |
| `views/TopologyEditor.vue` | 连线标签弹窗（双向端口/IP/VLAN/MTU/箭头）、分组容器、MiniMap、自动布局、导出到命令工作台 |
| `App.vue` | 深色侧边栏导航（已还原为横导航） |
| `stores/topology.ts` | 新增 `exportedDevices` + `TopologyDevice` + `TopologyPort` 接口 |
| `api/index.ts` | `GenerateFullReq` 增加 `vrp_version` |

### 五、已安装技能

| 技能 | 来源 | 说明 |
|---|---|---|
| **PUA v2.9.0** | SkillHub / @tanweai | 高压推动式调试，失败次数累计升级机制 |

### 六、项目规划文档更新

- 去除会员/支付功能
- 新增「拓扑编辑器详细设计」章节（§六）
- 新增「竞品调研与借鉴」章节（§十）
- 重写「分阶段开发计划」（§九）
- 新增本日志（§十三）

### 七、当前问题（待解决）

| 问题 | 状态 |
|---|---|
| 拓扑项目 Tab 在项目中点击后未显示设备列表 | ⚠️ 需调试 |
| 测试验证不足 | 🔵 部分功能未端到端测试 |

---

## 十四、2026-06-02 开发日志（完整记录）

> 本日累计修改/新增文件：**约 35 个文件**

### 一、P0 增强（撤销/重做/导出PNG/导出Excel/模板扩充）

| 文件 | 操作 | 说明 |
|---|---|---|
| `views/TopologyEditor.vue` | 修改 | Graph history 启用 + Ctrl+Z/Y 快捷键 + exportPNG |
| `views/Generator.vue` | 修改 | 导出 Excel 按钮 + xlsx 库生成多厂商对比表 |
| `data/templates/*.json`（12 个新） | **新增** | 锐捷(4套)/迈普(4套)/华为(2套)/H3C(2套)/RouterOS(2套) |
| `data/templates/index.ts` | 修改 | 模板注册从 15→30 套 |

### 二、RouterOS (MikroTik) 全线接入

| 文件 | 操作 | 说明 |
|---|---|---|
| `engine/vendors/routeros/__init__.py` | **新增** | 6 大特性生成器 + V6/V7 版本区分 |
| `engine/vendors/routeros/version_config.py` | **新增** | V6/V7 命令差异映射（OSPF/BGP/WireGuard/Container） |
| `engine/adapters/routeros.py` | **新增** | 适配器 + ros_version 参数 |
| `engine/factory.py` | 修改 | 注册 RouterOSAdapter |
| `api/generate.py` | 修改 | vrp_version → ros_version 传参 |
| `data/manual/routeros.py` | **新增** | 86 条 RouterOS 命令速查 |
| `api/manual.py` | 修改 | routeros 厂商 + V6/V7 版本注册 |
| `views/Manual.vue` | 修改 | RouterOS 选项 |
| `views/Generator.vue` | 修改 | RouterOS V6/V7 版本选择器 + 专属表单 Tab |
| `components/generator/RosBasicForm.vue` | **新增** | RouterOS 系统设置（SSH/WinBox/DNS/NTP/用户） |
| `components/generator/RosInterfaceForm.vue` | **新增** | Bridge/物理接口/IP/VLAN过滤 |
| `components/generator/RosRoutingForm.vue` | **新增** | PCC 多线负载均衡（完整 WinBox 兼容） |
| `components/generator/RosFirewallForm.vue` | **新增** | NAT/DNAT/防火墙规则/WireGuard |
| `components/topology/deviceIcons.ts` | 修改 | RouterOS 图标注册 |
| `components/topology/PropertyPanel.vue` | 修改 | RouterOS 复用路由器表单 |
| `public/icons/routeros.svg` | **新增** | 红色 RouterOS 图标 |

### 三、版本命令完整性

| 文件 | 操作 | 说明 |
|---|---|---|
| `api/manual.py` | 重写 | 版本过滤参数 + 四厂商版本体系 + 扁平化方法 |
| `data/manual/huawei.py` | 修改 | 堆叠(stack)/CSS 集群标注版本（V5/V8/V300） |
| `data/manual/h3c.py` | 修改 | MAD 合并检测/BFD Echo 标注 Comware V7 |
| `data/manual/maipu.py` | 修改 | MPLS QoS 标注 MyPower V8 |
| `engine/vendors/huawei/security.py` | 修改 | ACL 语法区分版本（acl number → acl） |
| `engine/vendors/huawei/routing.py` | 修改 | 新增 VRRP 生成器 |
| `engine/vendors/h3c.py` | 修改 | Comware V5/V7 ACL/SSH 区分 + VRRP |
| `engine/vendors/h3c_version.py` | **新增** | ComwareVersion + get_cmd 版本映射（解决 h3c 目录冲突） |
| `web/src/api/index.ts` | 修改 | 类型支持 V6/V7/RouterOS |

### 四、路由器/交换机分离（核心架构改动）

| 文件 | 操作 | 说明 |
|---|---|---|
| `views/Generator.vue` | 重写 | `isRouterScene` 判断 → 路由器/交换机/RouterOS 三套 Tab |
| `components/generator/RouterWanForm.vue` | **新增+重写 3 次** | 厂商感知 WAN 表单，逐步向导 + ? 提示 + 命令预览 |
| `components/generator/HuaweiRoutingForm.vue` | **新增+重写 2 次** | OSPF/BGP/RIP/VRRP 专属 |
| `components/generator/H3cRoutingForm.vue` | **新增+重写 2 次** | 静态路由/OSPF/BGP/VRRP |
| `components/generator/RuijieRoutingForm.vue` | **新增+重写 2 次** | 静态路由/OSPF/BGP |
| `components/generator/MaipuRoutingForm.vue` | **新增+重写 2 次** | 静态路由/OSPF/BGP |
| `engine/vendors/huawei/wan.py` | **新增** | PPPoE/静态IP/DHCP/NAT/DNAT/DHCP-Server 6 大生成器 |
| `engine/adapters/huawei.py` | 修改 | `generate_full` 增加 wan 字段处理 |
| `engine/vendors/h3c.py` | 修改 | `generate_wan_config()` → PPPoE/NAT/DNAT/DHCP |

### 五、命令准确性验证

对照华为 AR 路由器官方文档（support.huawei.com）修正了以下错误：

| 修正项 | 错误 | 正确（官方） |
|---|---|---|
| ACL 号 | `acl 3998 rule 10 permit ip` | `acl number 2000 rule 5 permit source x.x.x.x 0.0.0.255` |
| Dialer | 缺 `dialer-group 1` | 补充 |
| PAP | 缺 | `ppp pap local-user xxx password simple xxx` |
| NAT outbound | `nat outbound 3998` | `nat outbound 2000` |
| DNAT | 正确 ✅ | `nat server protocol tcp global current-interface 443 inside x.x.x.x 443` |

### 六、Bug 修复记录

| Bug | 耗时 | 解决 |
|---|---|---|
| `::root` → `:root` CSS 伪类错误导致主题变量失效 | 5min | App.vue 修正 |
| Vue 3 `v-model` 不能用在 v-for 迭代变量上 | 20min | 4 个路由表单全部改为 `form.xxx[i].yyy` 索引访问 |
| HuaweRoutingForm 误写空文件 | 5min | 重新创建完整文件 |
| H3C `h3c/version_config.py` 与 `h3c.py` 包冲突 | 10min | 改为 `h3c_version.py` 平级文件 |
| 后端旧进程未重启导致新功能不可用 | 多次 | 手动 kill + uvicorn reload |

### 七、遇到的困难

| 困难 | 说明 |
|---|---|
| **PCC 语法差异** | RouterOS PCC 的 per-connection-classifier 格式为 `both-addresses:TOTAL/BUCKET`（总在前），而非直觉的 `BUCKET/TOTAL`。需对照 WinBox 截图修复。 |
| **Rust 编译卡死** | `npm install` 时 xlsx 包编译 native 模块超时，须等 2-3 分钟 |
| **多厂商表单架构** | 需要同时支持 5 厂商 × 2 设备类型 × 多 Tab 的动态切换，模板层容易漏传 props |
| **命令验证** | 华为/H3C 开放文档需要 fetch+解析，小部分页面为 SPA 渲染无法直接抓取 |
| **v-model v-for 陷阱** | Vue 3 编译器不允许 `v-model="item"` 当 item 是 v-for 迭代变量，4 个表单全部中招 |

### 八、待完成事项

| 优先级 | 事项 | 状态 |
|---|---|---|
| P0 | 华三/锐捷/迈普 WAN 后端生成器（目前仅华为完整） | 🔴 部分完成 |
| P1 | 路由器 SecurityForm 改为 ACL/防火墙策略（而非当前端口安全） | 🟡 表单已改 Tab 名为 ACL/防火墙 |
| P1 | 基础配置表单增加路由器专属提示（当前为交换机通用） | 🟡 待做 |
| P1 | 配置下发（SSH 直推） | 🔴 未开始 |
| P1 | 内置 SSH 终端 | 🔴 未开始 |
| P2 | 命令模拟器验证（华为 eNSP 验证生成的命令） | 🔴 未开始 |
| P2 | 参数校验增强（VLAN ID/IP 冲突） | 🔴 未开始 |
| P3 | 锐捷/迈普 WAN 无需 CLI 的纯 Web 指引页面 | 🟡 RouterWanForm 已加 alert 提示 |

---

## 附录 A：实际项目结构

```
g:\NetCmdGen\
├── 项目规划.md              ← 当前文档
├── README.md               ← 项目介绍
├── docs\                   ← 详细设计文档
│   ├── NetOps-toolkit复用方案.md
│   └── 开源项目复用分析.md
├── web\                    ← 前端 Vue 3 项目（Vite + Element Plus + AntV X6）
│   ├── src\
│   │   ├── views\          ← 页面（TopologyEditor/Generator/Manual/Tools）
│   │   ├── components\     ← 组件（topology/ 子目录）
│   │   ├── stores\         ← Pinia 状态（topology.ts / vendor.ts）
│   │   ├── api\            ← API 请求封装
│   │   ├── router\         ← 路由配置
│   │   ├── assets\         ← 静态资源（SVG 图标放在 icons/devices/）
│   │   └── utils\          ← 工具函数
│   ├── package.json
│   └── vite.config.ts
├── api\                    ← 后端 Python FastAPI 项目
│   ├── app\
│   │   ├── main.py         ← FastAPI 入口
│   │   ├── api\            ← 路由层（generate/manual/tools_*）
│   │   ├── engine\         ← 命令生成引擎
│   │   │   ├── base.py     ← VendorAdapter Protocol
│   │   │   ├── factory.py  ← 适配器工厂
│   │   │   ├── adapters\   ← 厂商适配器（huawei/h3c/ruijie/maipu）
│   │   │   ├── vendors\    ← 厂商生成器（底层实现）
│   │   │   └── templates\  ← 场景模板（campus.py 等）
│   │   ├── core\           ← 核心工具（validator.py 等）
│   │   ├── data\           ← 静态数据（manual/ 命令速查 4 厂商）
│   │   └── tools\          ← 网络工具实现
│   ├── tests\
│   └── requirements.txt
├── templates\              ← 命令模板（YAML，待扩展）
├── opensource\             ← 开源项目引用
└── scripts\                ← 辅助脚本
```

## 附录 B：参考资源
- AntV X6 拓扑画图：https://x6.antv.antgroup.com/
- drawio 嵌入：https://github.com/jgraph/drawio
- 各厂商命令手册：官方支持网站（华为 support.huawei.com、H3C www.h3c.com、思科 cisco.com 等）
- 网络模拟器：华为 eNSP、Cisco Packet Tracer、GNS3、EVE-NG
